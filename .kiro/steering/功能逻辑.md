# 集群管理平台 - 功能逻辑文档

> **版本**: v3.0.0  
> **最后更新**: 2026-03-12  
> **说明**: 完全重写，基于当前实际功能模块

---

## 📋 文档说明

本文档记录集群管理平台的核心功能模块、关键技术实现和部署信息。

**核心功能模块**:
1. 硬件告警诊断系统
2. AI智能查询系统
3. 监控分析模块 (BCC/BOS/EIP/PFS)
4. CMDB资源管理
5. 运营分析报告
6. 系统配置管理

---

## 🚨 硬件告警诊断系统

### 系统概述

自动监控告警文件、解析告警信息、匹配故障手册、调用CCE诊断API、生成AI解读并发送Webhook通知。

### 核心流程

```
文件监控 → 告警解析 → 手册匹配 → AI解读 → Webhook通知
                ↓
         (CCE集群) → 诊断API → 后台线程等待 → 增强AI解读 → 二次通知
```

### 关键功能

**1. 文件监控**
- 监控目录: `/app/alerts_source` (宿主机只读挂载)
- 监控模式: PollingObserver (轮询避免inotify限制)
- 配置管理: 数据库`monitor_paths`表动态配置
- 去重机制: Redis文件级锁 (TTL 1小时)

**2. 告警解析**
- 支持多行JSON数组格式
- 自动识别CCE集群 (cluster_id以'cce-'开头) 和物理机
- 提取: alert_type, component, severity, ip, cluster_id

**3. 故障手册匹配**
- 数据来源: `fault_manuals`表 (290条记录，CSV导入)
- 匹配规则: alert_type + component
- 返回: 故障名称、影响、恢复方案、解决方案

**4. CCE诊断API (仅CCE集群)**
- 调用: 百度云CCE节点诊断接口
- 诊断项: 73个检查项 (Annotation/GPU/Hardware/Node等)
- 后台线程: 独立事件循环，15秒轮询，最大等待15分钟
- 复用机制: 15分钟窗口内同节点复用诊断结果

**5. AI解读**
- 模型: 厂内ERNIE 4.5 Turbo 32K
- 输入: 告警信息 + 手册匹配 + (可选)API诊断
- 输出: Markdown格式分析报告

**6. Webhook通知**
- 支持: 飞书 (HMAC-SHA256签名) / 如流 (group_id)
- 分段发送: 基础信息 + AI解读 (避免消息截断)
- 过滤: severity_filter, component_filter

### 数据库设计

**alert_records**: 告警记录 (alert_type, ip, cluster_id, severity, timestamp, raw_data)
**diagnosis_results**: 诊断结果 (manual_matched, api_task_id, api_diagnosis, ai_interpretation, notified)
**fault_manuals**: 故障手册 (name_zh, alert_type, component, solution, impact, recovery)
**webhook_configs**: Webhook配置 (type, url, secret/group_id, severity_filter)
**monitor_paths**: 监控路径配置 (path, file_pattern, priority, enabled)

### 前端功能

**告警列表** (`/alerts/list`):
- 查询、排序、筛选
- 批量选择、补发通知
- 快速创建iCafe卡片
- 编辑告警字段 (cluster_id, component, severity等)

**告警详情** (`/alerts/:id`):
- 告警基础信息
- 手册匹配结果
- CCE诊断报告 (73项详情)
- AI解读报告
- 编辑和重新诊断功能

**配置管理** (`/system-config`):
- 监控路径配置
- Webhook配置
- iCafe配置

### 关键技术点

**1. 重新诊断功能** (2026-03-12修复)
- 用户可手动修正cluster_id等字段
- 点击"重新诊断"删除旧诊断结果，重新执行完整流程
- 使用主键重新查询避免Session缓存问题

**2. 后台线程诊断** (2026-03-12修复)
- 使用`logger.warning()`确保生产环境日志可见
- 独立Session和事件循环避免阻塞
- 使用`flush()+commit()`确保数据库更新成功

**3. 三层去重机制**
- 文件级: Redis SETNX锁
- 节点级: Redis诊断锁 (15分钟窗口)
- 数据库级: alert_type + ip + timestamp唯一性检查

---

## 🤖 AI智能查询系统

### 系统架构

```
用户查询 → 意图路由 → 执行处理 → 返回结果(SSE流式)
```

### 核心组件

**1. Embedding模型**
- 地址: `http://10.175.96.168:8001`
- 模型: Gemini3Pro (768维向量)

**2. ERNIE API**
- 模型: ernie-4.5-turbo-32k
- 用途: 意图分类、SQL生成、自然语言生成

**3. 向量存储 (FAISS)**
- Schema向量: 数据库表结构
- 知识库向量: 知识条目
- 报告向量: 分析报告

### 意图路由

**三层策略**:
1. 强制规则: IP地址/实例ID检测 (置信度0.95)
2. 路由规则知识库: 向量+关键词检索，RRF融合
3. AI分类: ERNIE API意图分类

**意图类型**: sql / rag_knowledge / rag_report / chat

### Schema RAG

- 服务启动时加载所有表结构并向量化
- 查询时检索top-5相关表 (相似度阈值0.3)
- 返回表结构用于SQL生成

---

## 📊 监控分析模块

### BCC实例监控

- 3-sigma异常检测
- 按使用率智能分组
- 扩容/降配建议
- 成本估算

### BOS存储分析

- 3-sigma存储增长检测
- 低使用率识别
- TOP10集中度分析

### EIP流量分析

- 3-sigma流量异常检测
- 丢包分析
- 带宽优化建议

### PFS监控模块

**功能**:
- 64+指标监控 (容量/吞吐/QPS/延迟/元数据)
- 集群级别 + 客户端级别指标
- 时间范围选择 (快捷/自定义/对比模式)
- 数据可视化 (ECharts图表)
- 异步导出 (CSV格式，MinIO存储)

**技术架构**:
- 后端: PFSPrometheusClient → Prometheus API
- 缓存: Redis 5分钟
- 存储: MySQL (tasks表) + MinIO (导出文件)

**配置**: 系统配置页面统一管理 (Grafana URL, Token, Instance ID)

---

## 📦 CMDB资源管理

### 数据模型

**IaasServer** (145字段): hostname, sn, brand, model, idc, rack, BNS, RMS, Nova Host
**IaasInstance** (80字段): uuid, name, status, Nova VM, Logical, CRM

### 功能

- API自动同步 (AMIS API)
- Cookie管理 (完整Cookie/BDUSS提取)
- 定时同步 (1-24小时可配置)
- 增量更新
- 同步日志记录

---

## 📈 运营分析报告

### 功能

- 趋势预测 (线性回归)
- 预测指标: 工作量、闭环数、解决率
- 置信度: R²值计算
- 人效分析
- iCafe集成 (快速创建卡片)

---

## ⚙️ 系统配置管理

### 配置模块

**监控配置**: BCC/BOS/EIP实例ID列表
**分析配置**: 运营分析时间范围、指标选择
**CMDB配置**: Cookie、同步间隔
**PFS配置**: Grafana URL、Token、Instance ID
**iCafe配置**: API地址、认证信息

### 存储

- 数据库: `system_config`表 (module, config_key, config_value)
- 格式: JSON
- 加密: 敏感信息加密存储

---

## 🔧 技术栈

| 层级 | 技术 |
|------|------|
| 后端 | FastAPI 0.104 / SQLAlchemy 2.0 / Pydantic 2.0 |
| 前端 | Vue 3 / Element Plus / ECharts / TailwindCSS |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis 7 |
| 存储 | MinIO |
| AI | 百度ERNIE 4.5 / Gemini3Pro Embedding |
| 向量检索 | FAISS |
| 文件监控 | Watchdog |
| 部署 | Docker + Docker Compose + Nginx |

---

## 🚀 部署说明

### 环境要求

- Docker 20.10+
- Docker Compose 2.0+
- 内存 8GB+
- 磁盘 20GB+

### 快速部署

**外网部署**:
```bash
./deploy.sh
```

**内网离线部署**:
```bash
# 外网打包
./pack-offline.sh

# 内网部署
tar -xzvf offline-deploy.tar.gz
cd offline-deploy
vi .env  # 修改CORS_ORIGINS
./start.sh
```

### 访问地址

- 前端: http://服务器IP:8089
- API文档: http://服务器IP:8089/docs
- 默认账号: admin / admin123

---

## 📞 服务器信息

| 项目 | 内容 |
|------|------|
| 内网服务器 | 10.175.96.168 |
| SSH访问 | ssh root@10.175.96.168 |
| 服务端口 | 8000 (API), 8089 (Web), 3306 (MySQL) |
| 容器名称 | cluster-backend, cluster-frontend, cluster-mysql, cluster-redis, cluster-minio |

---

## 📝 最近重要更新

### 2026-03-12 - 重新诊断功能和后台线程修复

**BUG-021**: 重新诊断使用旧cluster_id
- 修复: 使用主键重新查询避免Session缓存

**BUG-022**: 诊断结果未更新到数据库
- 修复: logger.warning日志 + 移除expire_all + flush+commit事务管理

### 2026-03-12 - 内网部署MySQL连接修复

- 增加数据库就绪检查 (确保cluster_management可访问)
- 额外等待5秒确保连接池稳定
- 失败时提供手动执行命令

### 2026-03-10 - iCafe集成和告警管理增强

- iCafe卡片创建集成
- 告警字段编辑功能
- 告警架构优化 (移除FileSyncService)

### 2026-02-26 - Webhook通知状态更新

- 修复notified字段未更新问题

### 2026-02-25 - 告警文件解析多行JSON支持

- 支持逐行解析JSON数组

### 2026-02-24 - 故障手册CSV导入

- 支持CSV格式导入 (290条记录)
- 自动重新匹配已有告警

---

**文档版本**: v3.0.0 | **最后更新**: 2026-03-12 📄
