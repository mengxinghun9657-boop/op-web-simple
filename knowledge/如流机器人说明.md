1. 接收配置
1.1. 设置接收服务地址​
管理员可以在应用中设置回调配置
参数
说明
URL
接收消息和事件服务器URL，由开发者搭建，建议使用HTTPS
Token
用于生成签名，可由企业任意填写
EncodingAESKey
用于消息体的加密，是AES密钥的Base64编码，长度固定为22个字符，从a-z、A-Z、0-9共62个字符中选取，解码后即为16字节长的AESKey
1.2. 验证URL的有效性​
当点击“保存”提交以上信息时，如流平台会发送一条POST验证消息到填写的URL，企业的接收消息服务器接收到验证请求后，需要作出正确的响应才能通过URL验证。 假设企业回调URL为https://example.com/。
请求地址：https://example.com
请求body：signature=5d03603ea6f77b5608d5735a914b3221×tamp=1543647265&rn=376117246&echostr=7cff9
参数说明：
参数
类型
参数位置
是否必须
说明
signature
String
Query参数
是
签名，用于验证调用者的合法性。具体算法见附录
timestamp
Integer
Query参数
是
时间戳
rn
Integer
Query参数
是
随机数
echostr
String
Query参数
是
随机字符串
接收消息服务器URL收到请求后，需要做如下操作：
通过参数signature对请求进行校验，确认调用者的合法性，详见附录； 在3秒内响应请求，响应内容为上一步得到的echostr值之后接入验证生效，接收消息开启成功。
1.3. 接收消息和事件​
订阅「用户在群聊中@机器人的消息」和「群聊系统事件」后， 机器人将会收听群聊用户@机器人的消息以及群操作事件，有关的消息格式和事件，请参阅后续订阅消息与事件部分。

2. 订阅消息与事件
用户在如流上操作会产生事件，机器人可以订阅这些事件来与如流进行业务高度整合，你可以在企业管理后台来开启订阅消息和事件，仅企业机器人可以订阅。
2.1. 如何订阅消息与事件
在开启订阅消息与事件功能之前，需要先创建一个企业机器人：
1、申请创建企业机器人，请参阅https://work.baidu.com/new-work/static/main/process-start/7506
2、在企业后台应用列表，选择你创建的企业机器人，点击进入机器人详情页，点击「接收消息服务器」进入设置，配置一个接收消息URL，并完成有效性验证后，即可接收到如流回调的消息和事件。
2.2. 接收消息与事件列表
每个应用只能配置一个接收消息URL，开启接收消息和事件后，以下所有事件触发后，都会发送到该URL。
开放平台推送 application/json 格式的通知事件，不同类型的事件的消息体内容有所不同，你可以通过事件 event type 字段来区分不同的事件通知。
事件名称
事件类型
事件描述
接收消息
MESSAGE_RECEIVE
同服务号对话，群里@机器人消息、使用/快捷命令发送消息时推送消息内容
群基本信息变更
GROUP_INFO_CHANGE
群基础信息变更，比如群名称修改
新增群管理员
GROUP_MANAGER_ADD
新增管理员时推送事件
取消群管理员
GROUP_MANAGER_DEL
取消管理员时推送事件
群成员加入
GROUP_MEMBER_ADD
群里有新加入成员时推送事件
群成员退出
GROUP_MEMBER_DEL
群里有成员退出时推送事件
添加机器人
GROUP_OPEN_APP
机器人被添加进群时推送事件
移除机器人
GROUP_CLOSE_APP
机器人被移除群聊时推送事件
点击菜单气泡
MENU_CLICK
用户点击菜单气泡事件
2.2.1. 添加机器人
当群管理员在企业群添加机器人时，平台推送GROUP_OPEN_APP事件到接收消息URL。
回调示例：
{
    "eventtype":"GROUP_OPEN_APP",
    "groupid":1780915,
    "corpid":"hiJdAbPeLKtETa",
    "agentid":575,
    "time":1586783116712 // 时间戳(ms)
}
2.2.2. 移除机器人
当群管理员在企业群移除机器人时，平台推送GROUP_CLOSE_APP事件到接收消息URL。
回调示例：
{
    "eventtype": "GROUP_CLOSE_APP",
    "agentid": 1,
    "groupid": 1235,
    "corpid": "hiJdAbPeLKtETa",
    "time": 1231212
}
2.2.3. 接收消息
消息事件包括与服务号直接对话，以及用户在群聊中“@机器人”、使用“/快捷命令”与机器人进行的对话。 当前应用默认可以接收机器人私聊消息以及使用“/快捷命令”的消息，接收群里中@机器人的消息，需要在企业管理后台开启「用户在群聊中@机器人的消息」权限。 当消息事件发生时，开放平台推送 message 消息到接收消息URL，不同类型的消息通知内容略有差异。
2.2.3.1. 文本消息
回调示例：
{
    "eventtype":"MESSAGE_RECEIVE",
    "agentid":871,
    "groupid":1609028,
    "corpid":"hi865cd7777dacfe67",
    "message":{
        "header":{
            "fromuserid":"gaohe02",
            "toid":1609028,
            "totype":"GROUP",
            "msgtype":"MIXED",
            "clientmsgid":1584734519361,
            "messageid":1661664132266662422,
            "msgseqid":"",
            "at":{
                "atrobotids":[

                ]
            },
            "extra":"",
            "servertime":1584686519902,
            "clienttime":1584686519877,
            "updatetime":0
        },
        "body":[
            {
                "type":"AT",
                "robotid":4100350459,
                "name":"test接收消息服务器"
            },
            {
                "type":"command",
                "commandname":"斜杠指令"
            },
            {
                "type":"TEXT",
                "content":"文本消息"
            }
        ]
    },
    "time":1584686519955
}
2.2.3.2. 链接消息
回调示例：
{
    "eventtype":"MESSAGE_RECEIVE",
    "agentid":371,
    "groupid":1609028,
    "corpid":"hi865cd7777edcfe67",
    "message":{
        "header":{
            "fromuserid":"gaohe02",
            "toid":1632128,
            "totype":"GROUP",
            "msgtype":"MIXED",
            "clientmsgid":1584686562535,
            "messageid":1661664297539979799,
            "msgseqid":"",
            "at":{
                "atrobotids":[

                ]
            },
            "extra":"",
            "servertime":1584686563078,
            "clienttime":1584686563053,
            "updatetime":0
        },
        "body":[
            {
                "type":"AT",
                "robotid":4100034559,
                "name":"test接收消息服务器"
            },
            {
                "type":"TEXT",
                "content":" "
            },
            {
                "type":"LINK",
                "label":"http://www.baidu.com/"
            }
        ]
    },
    "time":1584686563176
}
2.2.3.3. 图片消息
回调示例：
{
    "eventtype":"MESSAGE_RECEIVE",
    "agentid":572,
    "groupid":1812300,
    "corpid":"hi53Ldm7uvm7zopMha",
    "message":{
        "header":{
            "fromuserid":"zhoumengqi01",
            "toid":1834600,
            "totype":"GROUP",
            "msgtype":"MIXED",
            "clientmsgid":1584686612372,
            "messageid":1661664345345670862,
            "msgseqid":"",
            "at":{
                "atrobotids":[

                ]
            },
            "extra":"",
            "servertime":1584686609095,
            "clienttime":1584686609057,
            "updatetime":0
        },
        "body":[
            {
                "type":"AT",
                "robotid":4100015525,
                "name":"自定义机器人"
            },
            {
                "type":"TEXT",
                "content":" "
            },
            {
                "type":"IMAGE",
                "downloadurl":"https://e4hi.im.baidu.com/proxy/download?taskId=8bbd0d2f-dad4-4aa5-9f53-12365baad7ff"
            }
        ]
    },
    "time":1584686609139
}
2.2.4. 引用回复的消息
{
    "eventtype": "MESSAGE_RECEIVE",
    "agentid": xxx,
    "groupid": xxx,
    "corpid": "hi6AGwqym4V3cV2LQ3",
    "message": {
        "header": {
            "fromuserid": "yangfeng09_rd",
            "toid": 1681387,
            "totype": "GROUP",
            "msgtype": "MIXED",
            "clientmsgid": 1751269673692,
            "messageid": 1836339350307196087,
            "msgseqid": "",
            "compatible": "",
            "offlinenotify": "",
            "extra": "",
            "servertime": 1751269674622,
            "clienttime": 1751269674594,
            "updatetime": 0
        },
        "body": [
            {
                "type": "replyData",
                "content": "111",
                "imid": 3000055438,
                "sBasemsgId": 1836339314504617142,
                "sMsgId2": 300010678,
                "replyImages": []
            },
            {
                "type": "TEXT",
                "content": "\n22"
            }
        ]
    },
    "time": 1751269674732,
    "fromid": 3000055438,
    "opencode": "4#dmVD3T5YB7OGcr3b/oqH8MkM3QnHpOfk+BIYZWwNqG4=",
    "messageXML": ""
}
2.2.5. 新增群管理员
回调示例：
{
    "eventtype": "GROUP_MANAGER_ADD",
    "agentid": 1,
    "groupid": 23445,
    "corpid": "hiJddbPeLKaEXa",
    "userids": ["lisi", "wangwu"],
    "time": 1231212121212 //ms
}
2.2.6. 取消群管理员
回调示例：
{
    "eventtype": "GROUP_MANAGER_DEL",
    "agentid": 1,
    "groupid": 23445,
    "corpid": "hiJddbPeLKaEXa",
    "agentid": 1,
    "userids": ["lisi", "wangwu"],
    "time": 1231212121212 //ms
}
2.2.7. 群成员加入
回调示例：
{
    "eventtype": "GROUP_MEMBER_ADD",
    "agentid": 1,
    "corpid": "hiJdAbPeLKtETa",
    "groupid": 23445,
    "grouptype": "NORMAL", // TOPIC话题群 NORMAL 普通群
    "parentgroupid": 2323232, //
    "app_extid ": 121212121,
    "topic_type_extid": 121212121, // 话题扩展id
    "userids": ["lisi", "wangwu"],
    "time": 1231212121212121 //
}
2.2.8. 群成员退出
回调示例：
{
    "eventtype": "GROUP_MEMBER_DEL",
    "agentid": 1,
    "corpid": "hiJddbPeLKaEXa",
    "groupid": 23445,
    "grouptype": "NORMAL",
    "parentgroupid": 2323232,
    "userids": ["lisi", "wangwu"],
    "time": 12312121212112 //ms
}
2.2.9. 群基本信息变更
表示群的基本属性变更时回调，比如群名称的更改、群头像变更、修改群公告等
回调示例：
{
    "eventtype": "GROUP_INFO_CHANGE",
    "agentid": 1,
    "corpid": "hiJdAbPeLKtETa",
    "groupid": 23445,
    "grouptype": "NORMAL",
    "parentgroupid": 2323232,
    "groupinfo": {
        "headmd5": "xxxxx",
        "desc": "",
        "bulletin": "群公告",
        "friendlylevel": "",
        "name": "群名称",
        "digest": "",
        "owneruserid": "zhangsan",
        "createtime": 1212121211, //ms
        "updatetime": 1500000000 //ms
    },
    "time": 12312121212121 //ms
}
2.2.10. 用户点击气泡
回调示例：
{
    "fromuserid":"testuser1",
    "agentid":3177,
    "corpid":"1",
    "eventkey":"key_530",
    "groupid":10855729,
    "eventtype":"MENU_CLICK",
    "time":"1747031150501",
    "ruid":4105000000
}
接收群聊中所有新消息
必读❗️
* 权限：调用此接口需要机器人管理员先开启权限【接收群聊中所有新消息】，申请入口：企业后台 > 应用中心 > 你的机器人 > 功能 > 机器人 > 设置 > 功能 > 群聊场景 > 开放API > 设置
* 提效工具：该接口已支持使用comate插件快速生成多种语言的接入代码，快去尝试下吧～Comate × 如流机器人插件：快速上线全新智能机器人
* 技术支持：@魏小伟

API协议
接收所有消息与普通的@消息的投递方式相同，如尚未接入，可以先参考：接收群聊中@机器人消息
协议区别为eventtype="ALL_MESSAGE_FORWARD"，各body体中新增messageXML字段，为消息全部信息，可自行解析。
{
    "eventtype":"ALL_MESSAGE_FORWARD",
    "agentid":871,
    "groupid":1609028,
    "corpid":"hi865cd7777dacfe67",
    "message":{
        "header":{
            "fromuserid":"gaohe02",
            "toid":1609028,
            "totype":"GROUP",
            "msgtype":"MIXED",
            "clientmsgid":1584734519361,
            "messageid":1661664132266662422,
            "msgseqid":"",
            "at":{
                "atrobotids":[

                ]
            },
            "extra":"",
            "servertime":1584686519902,
            "clienttime":1584686519877,
            "updatetime":0
        },
        "body":[
            {
                "type":"AT",
                "robotid":4100350459,
                "name":"test接收消息服务器"
            },
            {
                "type":"TEXT",
                "content":" 文本消息"
            },{
                "type": "FILE",
                "name": "start.sh",
                "md5": "",
                "fid": "xxxxxxxxxxxxxx",
                "size": 2672,
                "downloadurl": ""
            }
        ]
    },
    "time":1584686519955
}
以机器人身份发送消息到群聊
必读❗️
* 权限：调用此接口需要机器人管理员先开启权限【以机器人身份发送消息到群聊】，申请入口：企业后台 > 应用中心 > 你的机器人 > 功能 > 机器人 > 设置 > 功能 > 群聊场景 > 开放API > 设置
* 接口用途：该接口主要用于以机器人身份发送消息到群聊。
* 提效工具：该接口已支持使用comate插件快速生成多种语言的接入代码，快去尝试下吧～Comate × 如流机器人插件：快速上线全新智能机器人
* 技术支持：@魏小伟

默认限流：10 QPS/机器人，如需特殊场景扩容qps请求联系@魏小伟 单独申请扩容
1. 请求基本信息
接口路径
/robot/msg/groupmsgsend
HTTP 方法
POST
权限要求
请求头header：Authorization = Bearer-app_access_token。详见获取app_access_token(如有问题咨询mayunfei02)
1.1. 各环境地址
preonline：http://xplatform-preonline.dev.weiyun.baidu.com/open-plat/api/devp/v1/robot/msg/groupmsgsend
线上：http://apiin.im.baidu.com/api/v1/robot/msg/groupmsgsend
工作卡各环境 API 访问地址
1.2. 请求头​
名称
类型
是否必须
描述
Content-Type
String
是
为固定值："application/json; charset=utf-8"
1.3. 请求体
示例：
{
    "message": {
        "header": {
            "toid": 1671623,
            "totype": "GROUP",
            "msgtype": "TEXT",
            "clientmsgid": 1569398477302,
            "role": "robot"
        },
        "body": [
            {
                "content": "测试",
                "type": "TEXT"
            }
        ]
    }
}
参数说明：
参数
类型
参数位置
是否必须
说明
toid
long
message.header
是
目标群组的ID
totype
String
message.header
是
消息发送的目标类型，此处为 “GROUP”
msgtype
String
message.header
是
消息的类型，此处为 “TEXT”
clientmsgid
long
message.header
是
客户端生成的一个唯一标识，用于避免消息重复发送
clienttime
long
message.header
是
客户端用户排重的唯一标识
如果不传，机器人服务会设置为当前的毫秒时间戳，传0会触发排重机制，建议业务自己维护唯一值
role
String
message.header
是
发送者的角色，此处为 “robot”
content
String
message.body[0]
是
实际发送的消息内容，此处为测试文本
type
String
message.body[0]
是
内容的类型，与上面的 msgtype 相对应，此处为 “TEXT”
1.4. 返回结果
示例：
{
    "code": "ok",
    "data": {
        "errcode": 0,
        "errmsg": "ok",
        "data": {
            "messageid": 1835260055958702611,
            "msgseqid": 300010003,
            "ctime": 1750240379318
        }
    }
}
参数说明：
参数
说明
errcode
错误代码，0 表示成功
errmsg
错误信息，"ok" 表示操作成功
data
包含返回的具体数据项
messageid
消息的唯一标识符，位于 data 对象内
msgseqid
消息的序列号，位于 data 对象内
ctime
消息的创建时间（Unix 时间戳），位于 data 对象内


2. 消息类型及数据格式​
"header": {
"toid": 123456, // 可选，接收方id, totype=GROUP时 toid为groupid
"totype": "SESSION", // 必须，会话类型，GROUP、SESSION
"sessionid": "12121212121212121", //可选，会话ID，totype="SESSION"时必填，表示以用户身份发消息
"msgtype": "TEXT", // 必须，消息类型 TEXT, IMAGE, RICHMEDIA, RICHMEDIA2,CMD
"clientmsgid": 1212121, // 必须，发送时唯一id，由请求方保证唯一，建议使用系统当前时间毫秒值
"role": "robot", // 必须，以机器人身份发送消息
}

2.1. 引用回复
机器人消息支持引用回复（TEXT、IMAGE、LINK）类型
参数节点与header和body同级，增加reply信息即可
"reply": {
    "messageid":"xxxxxxxxxxx", // 被回复引用的消息id
    "preview":"引用消息原文预览", // 业务方传入被引用消息的预览，如“文字内容……”、[图片]、[文件]
    "imid":"410xxxxxx", // 机器人imid
    "replytype":"1",  //string类型，"1-回复，2-引用"，不可传，默认值"1"
    
}
2.2. 文本类型（TEXT） 
消息示例：
[图片]

代码示例：
curl --location 'http://xplatform-preonline.dev.weiyun.baidu.com/open-plat/api/devp/v1/robot/msg/groupmsgsend' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer-at_1cc1dca9-712f-47cf-8e3d-209acf2cab93' \
  --data-raw  '{
    "agentid": 736,
    "message": {
        "header": {
            "toid": 1608555,
            "totype": "GROUP",
            "msgtype": "TEXT",
            "clientmsgid": 1569398477300,
            "role": "robot"
        },
        "body": [
            {
                "type": "TEXT",
                "content": "Hi~"
            }
        ]
    }
}'

2.3. 图片类型（IMAGE） 
消息示例：
[图片]

代码示例：
curl --location 'http://xplatform-preonline.dev.weiyun.baidu.com/open-plat/api/devp/v1/robot/msg/groupmsgsend' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer-at_1cc1dca9-712f-47cf-8e3d-209acf2cab93' \
  --data-raw  '{
    "agentid": 736,
    "message": {
        "header": {
            "toid": 1608555,
            "totype": "GROUP",
            "msgtype": "IMAGE",
            "clientmsgid": 1569398477302,
            "role": "robot"
        },
        "body": [
            {
                "content": "A+QIGv8u3c6YCCiiggAIKKKCAAgoooIACCiiggAIKKKCAAgoooIACCiiggAIKKKCAAgoooIACCiiggAIKKFAjAv8L0+vxkA8rL2IAAAAASUVORK5CYII=" // 图片byte数据base64编码后的值，byte小于1m
                "type":"IMAGE"
            }
        ]
    }
}'

2.4. 链接类型（LINK）
消息示例：
[图片]

代码示例：
curl --location 'http://xplatform-preonline.dev.weiyun.baidu.com/open-plat/api/devp/v1/robot/msg/groupmsgsend' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer-at_1cc1dca9-712f-47cf-8e3d-209acf2cab93' \
  --data-raw  '{
    "agentid": 736,
    "message": {
        "header": {
            "toid": 1608555,
            "totype": "GROUP"
            "msgtype": "TEXT",
            "clientmsgid": 1569398477303,
            "role": "robot"
        },
        "body": [
            {"type": "LINK", "href": "example.com"}
        ]
    }
}'

2.5. @群成员
消息示例：
[图片]

@人的代码示例：
curl --location 'http://xplatform-preonline.dev.weiyun.baidu.com/open-plat/api/devp/v1/robot/msg/groupmsgsend' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer-at_1cc1dca9-712f-47cf-8e3d-209acf2cab93' \
  --data-raw  '{
    "agentid": 736,
    "message": {
        "header": {
            "toid": 1608555,
            "totype": "GROUP",
            "msgtype": "TEXT",
            "clientmsgid": 1569398477312,
            "role": "robot"
        },
        "body": [
            {
                "type": "TEXT",
                "content": " 以下人员注意啦～ "
            },
            {
                "type": "AT",
                "atall": false, // true-表示at全体成员，此时atuserids失效
                "atuserids": [ // 成员ID
                    "linli",
                    "fanghan"
                ]
            }
        ]
    }
}'
@机器人的代码示例 
curl --location 'http://xplatform-preonline.dev.weiyun.baidu.com/open-plat/api/devp/v1/robot/msg/groupmsgsend' \
--header 'Authorization: Bearer-at_1cc1dca9-712f-47cf-8e3d-209acf2cab93' \
--header 'Content-Type: application/json' \
--data '{
    "message": {
        "header": {
            "toid": 1682832,
            "totype": "GROUP",
            "msgtype": "TEXT",
            "clientmsgid": 1569398477302,
            "role": "robot"
        },
        "body": [
            {
                "content": "测试",
                "type": "TEXT"
            },
            {
                "type": "AT",
                "atall": false,   
                "atagentids": [
                    1271,
                    1002,
                    1282    
                ]
            }
        ]
    }
}'

补充说明
- 机器人不能@自己（发消息的机器人）
- 被@的机器人必须在群内
- 被@的机器人不能是自定义机器人
- 单次支持@最多10个机器人

2.6. Markdown类型（MD）
具体格式见：机器人MD消息语法说明
消息示例：
[图片]
[图片]

MD消息支持@人，发送格式见以下示例
curl --location 'http://xplatform-preonline.dev.weiyun.baidu.com/open-plat/api/devp/v1/robot/msg/groupmsgsend' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer-at_1cc1dca9-712f-47cf-8e3d-209acf2cab93' \
  --data-raw  '{
    "message": {
        "header": {
            "toid": 11084351,
            "totype": "GROUP",
            "msgtype": "MD",
            "clientmsgid": 1569398477302,
            "role": "robot"
        },
        "body": [
            {
                "type": "AT",
                "atuserids":["shenyue04","lixuejian04","v_liuxiao17","liaozhibing", "dingxuefei", "chaihongxia"]
            },
            {
                "type":"MD",
                "content":"##### 测试机器人MD消息@人 \n实时新增用户反馈<font color=\"red\">188例</font>，请相关同事注意。\n**渠道来源**\n>*Web站*：<font color=\"green\">98例</font>\n>*Appstore*：<font color=\"gray\">90例</font> @shenyue04 @lixuejian04 @v_liuxiao17 @liaozhibing @dingxuefei @chaihongxia"
            }
       
        ]
    }
}'
MD消息支持@机器人，发送格式见以下示例
curl --location 'http://xplatform-preonline.dev.weiyun.baidu.com/open-plat/api/devp/v1/robot/msg/groupmsgsend' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer-at_1cc1dca9-712f-47cf-8e3d-209acf2cab93' \
--data-raw '{
    "message": {
        "header": {
            "toid": 1682832,
            "totype": "GROUP",
            "msgtype": "MD",
            "clientmsgid": 1569398477302,
            "role": "robot"
        },
        "body": [
            {
                "type": "AT",
                "atall": false,
                "atagentids": [    
                    1282
                ]
            }
            ,
            {
                "type": "MD",  
                "content": "## 问题内容 \n @1282你好，麻烦帮忙看下这个问题"       
            }
        ]
    }
}'

补充说明
- 机器人不能@自己（发消息的机器人）
- 被@的机器人必须在群内
- 被@的机器人不能是自定义机器人
- 单次支持@最多10个机器人

Markdown支持如下语法子集： 1、标题：1-6级标题，# 号和文本之间要有一个空格
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
2、粗体：

**加粗文本**
3、斜体：

*斜体文本*
4、引用：

>引用文本
5、字体颜色：只支持三种内置颜色：green（绿色）、gray（灰色）、red（红色），注意：color=后要用双引号，不要用单引号，用单引号字体颜色不会变

<font color="green">绿色</font>
<font color="gray">灰色</font>
<font color="red">红色</font>
6、链接:

[百度一下]（https://www.baidu.com/）
https://www.baidu.com/
7、有序列表：1. 和文本之间要加一个空格

1. 有序列表
2. 有序列表
3. 有序列表
8、无序列表：- 和文本之间要加一个空格

- 无序列表
- 无序列表
9、行内代码：
2.7 指令消息
指令消息示例
[图片]
机器人指令消息代码示例 
curl --location 'http://xplatform-preonline.dev.weiyun.baidu.com/open-plat/api/devp/v1/robot/msg/groupmsgsend' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer-at_1cc1dca9-712f-47cf-8e3d-209acf2cab93' \
--data '{
    "message": {
        "header": {
            "toid": 11934639,
            "totype": "GROUP",
            "msgtype": "TEXT",
            "clientmsgid": 1569398477302,
            "role": "robot"
        },
        "body": [
            {
                "content": "ecom_op",
                "type": "TEXT"
            }, 
            {
                "type": "CMD",
                "commandname": "help",
                "targetAgentId": 2712
            }
        ]
    }
}'

补充说明
- 机器人不支持使用自己的指令发起调用
- 单次发送指令消息时，最多使用一个指令
- 指令消息被调用方不能是自定义机器人
- 指令消息默认限流5qps
3. 消息发送频率限制​
为了保障群成员使用体验，以防收到大量消息的打扰，机器人发消息限制每个机器人每群20条/分钟，超出后接口将返回警告错误码，将限流5分钟。限流期间的消息将会被丢弃。
4. 返回错误码释义
errorcode
释义
-1
系统错误
0
OK
40000
参数错误
40035
群聊ID不合法
40036
群聊非企业群
40040
参数错误
40044
机器人未被添加到群中
40045
agentId不合法
40046
发送消息频率超限
40047
文件上传失败
40060
body超过9k
40061
text类型文本总长度超过2k
40062
link类型单个链接长度超过1k
40063
image类型图片数量超过1个
40064
header中offlinenotify长度超过1k
40065
header中compatible长度超过1k
40066
image类型图片大小超过1m
40067
markdown类型数量超过1个
40068
markdown内容长度超过2048个字符
40069
message属性格式不正确
40071
at超过50人
40200
机器人发送消息权限已被封禁
40201
机器人接受消息权限已被封禁
40300
机器人已被停用
机器人交互消息更新方案
如果你不了解什么是交互消息，可以跳转机器人交互消息介绍与示例
可进群体验 交互消息Demo效果 机器人交互消息展示群
前言
当前机器人发出来的交互消息支持在原卡片上更新，大致链路如下：
[流程图]
也就是说，对于机器人开发者而言，如果想要使用此功能，你需要准备的是
1. 配置好机器人的回调地址（https://qy.baidu.com/doc/index.html#/inner_serverapi/robot?id=接收消息）
2. （如果你想要刷新原有卡片的数据）回调地址需要给返回一个新的卡片数据，格式与发送交互消息时的body完全相同

综上，对于机器人开发者，需要参照下方回调地址配置配置一个url，并且该url能够接收并处理回调请求参数中的内容，最终按照回调接口返回格式进行返回。
回调地址配置
参考https://qy.baidu.com/doc/index.html#/inner_serverapi/robot?id=接收消息
回调请求参数
卡片上触发回调事件时，服务最终会往机器人注册的url发送一个POST请求，请求体采用json格式内容，包含以下内容
{
    "fields": "", // 卡片上特定区域的内容，当前只支持显示成员选择器上的内容。具体格式参见下方文档fields读取
    "buttonPosition": "2_1_button", // 代表点击的是第二行第一列
    "eventKey": "clickOnMenu", // 回传机器人业务方在callBack类型的按钮中设置的eventKey
    "userId": "xukefan", // 点击者的uuap 
    "toId": "1231414", // 消息接受方的唯一id
    "toType": "group", // 消息接受方的类型，目前只支持群消息
    
    "appContext": {
        // Map<String, Object>格式
        // 透传发送卡片时的数据
    }
}
fileds读取
fileds中的数据格式如下
[{
    "name": "contact",
    "value": "{
                \"contacts\":[{
                        \"corpid\":\"1\",
                        \"userid\":\"xionghaitao\",
                        \"uid\":\"3000055516\",
                        \"id\":3000055516,
                        \"realName\":\"xionghaitao_rd\",
                        \"bdid\":\"xionghaitao_rd\",
                        \"email\":\"xionghaitao@baidu.com\",
                        \"avatarMD5\":\"0e9258d4d65659027d3cbe2c49523cd7\",
                        \"type\":\"person\",
                        \"name\":\"xionghaitao_rd\",
                        \"staffDepartmentName\":\"新测试企业1022-parent\"
                   }]
             }"
}]
如果从fileds中获取成员选择器中的成员的uuap集合可以参考以下代码
最终会得到内容如"xukefan, xukefan01,xukefan02"
        StringBuilder sb = new StringBuilder();
        try {
            JSONArray outerArray = JSON.parseArray(fileds);
            for (int i = 0; i < outerArray.size(); i++) {
                JSONObject innerObject = outerArray.getJSONObject(i);
                if ("contact".equals(innerObject.get("name"))) {
                    String valueStr = innerObject.getString("value");

                    // 解析value字段中的JSON
                    JSONObject valueObj = JSON.parseObject(valueStr);
                    JSONArray contacts = valueObj.getJSONArray("contacts");

                    // 遍历contacts数组，提取userid
                    for (int j = 0; j < contacts.size(); j++) {
                        JSONObject contact = contacts.getJSONObject(i);
                        String singleUserid = contact.getString("userid");
                        if (i > 0) {
                            sb.append(",");
                        }
                        sb.append(singleUserid);
                    }
                }
            }
        } catch (Exception e) {

            log.error("解析inputFields出错, caused by {}", e.getMessage(), e);
        }
        String userids = sb.toString();
回调接口返回格式
{
    "code": 200, // code为200时才会变更现有卡片上的数据，非200时data不生效，只有msg生效
    "msg": "", // 点击按钮后弹出的toast文本
    "data": "" // 新的卡片数据，格式参考卡片发送文档中的整个接口请求体
}
data的格式可以参考机器人向群聊发送可交互消息接口中的格式，或者跳转查看data对应的java类

机器人交互消息介绍与示例
前言
什么是交互消息
传统的消息是纯文本格式，仅支持广播式推送，缺乏双向交互。当消息要引导用户跳转到其他系统做操作，整体的交互链路较长。
交互消息是一种可以在消息上进行操作，并且可以实时地显示操作效果的消息；同时可以选择将操作行为通知给开发者自己的业务。开发者可以根据业务的需求定义整个交互的链路，在消息上实现各种交互，做到消息即服务。

我们提供什么

备注
快捷的发送渠道
在原有的机器人消息发送渠道上，只需要更换请求中的json体即可发出一条交互消息
可变的消息布局
可以根据json体中的内容自动呈现相应的布局
多样的正文内容
消息中支持展示图片，文字链，markdown
消息的回调链路
支持用户的交互触发回调
消息的刷新机制
支持用户的交互触发消息内容的刷新
其他的通用交互
包括链接跳转、跳转联系人窗口等

Demo功能体验
机器人交互消息展示群 
点击进群，即可通过点击menu气泡（⬇️）体验功能
[图片]

交互消息布局介绍
交互消息将以一个卡片的形式呈现，消息中主要分为内容区➕交互区（见下图）
* 内容区
    * @人区：消息支持@全体成员或@最多5个成员。@人消息始终处于卡片最上方
    * 文本区：本文区域支持文字、文字链、图片、Markdown

* 交互区
    * 按钮区：交互区域由按钮构成，最多有8行3列按钮。按钮支持配置多种类型的事件，包括跳转链接、端JS能力、事件回调（可用于更新卡片数据）
        * 事件回调补充说明：可以通过对按钮进行配置，使该按钮触发卡片数据时，实现千人一面或千人千面（见下方示例）。



[图片]
[图片]
[图片]
交互消息效果示例

示例一、（事件回调）（卡片刷新）按钮千人千面效果展示
初始消息
[图片]
用户点击“帮我选一个”按钮后，只有该用户的界面数据发生变化
点击“帮我选一个”的用户界面
未操作的用户界面
[图片]
[图片]


示例二、（事件回调）（卡片刷新）按钮千人一面效果展示
初始消息
[图片]

用户点击“认领”按钮后，全体用户的界面数据发生变化
点击“认领”的用户界面
未操作的用户界面
[图片]
[图片]


示例三、（端JS能力）以“打开联系人窗口”为例
初始消息
[图片]

用户点击“打开联系人详情”按钮（按钮中预设了xianghaitao_rd的联系方式）
自动打开了与xionghaitao_rd的聊天界面

附录
PDF版下载

签名signature校验
signature是签名，用于验证调用者的合法性

校验流程：

将rn、timestamp、Token三个参数字符串拼接成一个字符串后进行md5
加密后获得的字符串与请求中的signature对比，标识该请求来源于如流平台
同时还可通过记录signature、timestamp防止重放攻击。

参数说明：

参数	类型	参数位置	是否必须	说明
rn	Integer	Query参数	是	请求中的rn
timestamp	Integer	Query参数	是	请求中的timestamp
Token	String	Query参数	是	管理后台应用详情-开发信息-回调配置获取的Token值
参考代码：

private static boolean checkSignature(HttpServletRequest request) throws Exception {
    String signature = request.getParameter("signature");
    String timestamp = request.getParameter("timestamp");
    String rn = request.getParameter("rn");
    String str = DigestUtils.md5Hex((rn + timestamp + Access_Token).getBytes("utf-8"));

    return signature.equals(str);
}
Click to copy
消息/事件内容解密
如流企业平台将消息或者事件明文经过AES加密，然后对加密后内容进行Base64编码成密文，密文为Encrypt_Msg = Base64_Encode(AES_Encrypt(msg))，msg为消息或事件明文

加密算法：AES-128 加密模式：ECB 填充方式：PKCS5（PKCS7兼容） 编码方式：Base64URLSafe（它不在末尾填充=号，并将标准Base64中的+和/分别改成了-和_）

解密流程：

Base64解码，Encrypt_Raw_Msg = Base64URLSafeDecode(Encrypt_Msg) 特别提醒：某些语言（比如Python）Base64URLSafeDecode方法不能处理尾部缺失=的情况，需要自行补齐=(len(Encrypt_Msg)% 4)
AES解密，msg = AES_Decrypt(Encrypt_Raw_Msg, AESKey)
参数说明：

参数	类型	参数位置	是否必须	说明
EncodingAESKey	String	RequestBody	是	企业平台生成，长度固定为22个字符
AESKey	String	RequestBody	是	AES算法的密钥
AESKey = Base64_Decode(EncodingAESKey + "==")，长度为16字节。
各语言示例： 主要包含signature校验和加解密示例，加密的消息或事件服务号消息和机器人消息本身内容的获取请查看对应文档。

JAVA示例

import org.apache.commons.codec.binary.Base64;
import org.apache.commons.codec.digest.DigestUtils;

String echostr = httpServletRequest.getParameter("echostr");
// 配置回调地址时应回调服务就绪，配置回调地址时会调用配置地址，需要回显才能校验通过
if (echostr != null && echostr.length() != 0) {
    if (checkSignature(request)) {
        httpServletResponse.getWriter().print(echostr);
    }
} else {
    byte[] msgBase64 = Base64.decodeBase64(IOUtils.toString(httpServletRequest.getInputStream(), "utf-8"));
    Charset charset = Charset.forName("utf-8");
    SecretKeySpec skeySpec = new SecretKeySpec(Base64.decodeBase64(encodingAESKey), "AES");
    Cipher cipher = Cipher.getInstance("AES/ECB/PKCS5Padding");
    cipher.init(Cipher.DECRYPT_MODE, skeySpec);
    byte[] decrypted = cipher.doFinal(msgBase64);
    // 通过AES解密后得到回调消息数据
    String msgjsonStr = new String(decrypted, charset);
}
Click to copy
Python示例

#!/usr/bin/python
# -*- coding: utf-8 -*-

import base64
import binascii

import six
# pip install pycrypto
# Windows No module named Crypto.Cipher问题，https://www.jianshu.com/p/09a14a61b454
from Crypto.Cipher import AES


def base64_urlsafe_decode(s):
    """
    base64 解码(urlsafe兼容模式)
    :return:
    """
    # 系统的urlsafe_b64decode方法不支持补'='
    s = s.replace('-', '+').replace('_', '/') + '=' * (len(s) % 4)

    return base64.b64decode(s)


class AESCipher(object):
    """
    AES加解密类
    """

    def __init__(self, key, mode=AES.MODE_ECB, padding='PKCS7', encode='base64', **kwargs):
        """
        初始化
        :param key:
        :param mode:
        :param padding: 数据填充方式 PKCS7、ZERO
        :param encode: 数据编码方式 raw、base64、hex
        """
        self.key = key
        self.mode = mode
        self.padding = padding
        self.encode = encode
        self.kwargs = kwargs

        self.bs = AES.block_size

        self.IV = self.kwargs.get('IV', None)
        if self.IV and self.mode in (AES.MODE_ECB, AES.MODE_CTR):
            raise TypeError("ECB and CTR mode does not use IV")

    def _aes(self):
        return AES.new(self.key, self.mode, **self.kwargs)

    def encrypt(self, plaintext):
        """
        加密
        :param plaintext:
        :return: py3返回 byte string, py2返回str
        """
        # padding https://en.wikipedia.org/wiki/Padding_(cryptography)#PKCS#5_and_PKCS#7
        if self.padding == 'PKCS7':
            pad = lambda s: s + (self.bs - len(s) % self.bs) \
                            * chr(self.bs - len(s) % self.bs).encode('utf-8')
        else:
            pad = lambda s: s + (self.bs - len(s) % self.bs) \
                            * '\x00'
        # 统一为字节类型
        if isinstance(plaintext, six.text_type):
            plaintext = plaintext.encode('utf-8')

        # 注意：加密、解密需单独实例化
        raw = self._aes().encrypt(pad(plaintext))

        if self.encode == 'hex':
            return binascii.hexlify(raw)
        if self.encode == 'base64':
            return base64.b64encode(raw)
        return raw

    def decrypt(self, ciphertext):
        """
        解密
        :param ciphertext:
        :return: py3返回 byte string, py2返回str
        """
        if not ciphertext:
            return None

        if self.padding == 'PKCS7':
            if six.PY3:
                unpad = lambda s: s[0:-s[-1]]
            else:
                unpad = lambda s: s[0:-ord(s[-1])]
        else:
            unpad = lambda s: s.rstrip('\x00')

        # 统一为文本字符类型
        if isinstance(ciphertext, six.binary_type) and self.encode != 'raw':
            ciphertext = ciphertext.decode('utf-8')
        if self.encode == 'hex':
            ciphertext = binascii.unhexlify(ciphertext)
        if self.encode == 'base64':
            ciphertext = base64_urlsafe_decode(ciphertext)

        return unpad(self._aes().decrypt(ciphertext))


if __name__ == '__main__':
    # flask示例(推荐>=1.1.0)，仅核心逻辑代码，其它web框架类比参考
    from flask import request

    # 获取POST参数echostr，Django可使用request.POST.get('echostr', None)
    # 注意：仅回调配置的时候有此参数，需要指定缺省值
    echostr = request.form.get('echostr', None)
    # 配置回调地址时应回调服务就绪，配置回调地址时会调用配置地址，需要回显才能校验通过
    if echostr:
        if check_signature(signature, rn, timestamp, token):
            return echostr
        else:
            return 'check signature fail'
    else:
        # 获取request raw body, Django可使用request.body
        msg_base64 = request.get_data()
        encrypter = AESCipher(base64_urlsafe_decode(encodingAESKey))
        # 通过AES解密后得到回调消息数据
        decrypted = encrypter.decrypt(msg_base64)
        msg_data = json.loads(decrypted)
Click to copy
PHP示例

<?php

class Encrypter
{
    private $method;
    private $password;
    private $padding;
    private $iv;
    private $lastErrorMessage;  // 最后一次失败的信息

    const AES_BLOCK_SIZE = 16;

    /**
     * Encrypter constructor.
     *
     * @param string $password aes_key
     * @param string $method 加密的模式
     * @param string $padding 填充数据模式, 支持ZERO、PKCS7（兼容PKCS5）
     * @param string $iv 初始向量, 默认使用AES-128-ECB初始向量为空
     */
    function __construct($password, $method = 'AES-128-ECB', $padding = 'PKCS7', $iv = '')
    {
        $this->password = $password;

        $this->method = $method;
        $this->padding = $padding;
        $this->iv = $iv;
    }

    /**
     * 加密
     *
     * @param string $data 需要加密的字符串
     *
     * @return false|string 失败时返回false
     */
    public function encrypt($data)
    {
        /**
         * openssl_encrypt ZERO_PADDING模式有个小问题
         * 当设置为OPENSSL_ZERO_PADDING时其实是禁用padding
         * EVP_CIPHER_CTX_set_padding(&cipher_ctx, 0);
         * 因此需要构造合适长度的被加密串, 这样加密过程中就不需要padding了，不然会加密失败
         * 不过需要注意的是因为padding了\0字符 解密完成后需要trim。显而易见,若是被加密串两端本身就包含\0等字符就会丢失信息
         *
         * 参考资料: http://php.net/manual/en/function.openssl-encrypt.php#117208
         * http://php.net/manual/en/function.openssl-encrypt.php#117499
         * http://www.cnblogs.com/solohac/p/4284424.html
         * http://stackoverflow.com/questions/11838197/php-vs-java-aes-encryption-what-is-the-difference
         */
        if ($this->padding === 'ZERO') {
            $options = OPENSSL_ZERO_PADDING;
            $padLen = self::AES_BLOCK_SIZE - (strlen($data) % self::AES_BLOCK_SIZE);
            $data .= str_repeat("\0", $padLen); // zero padding
        } else {
            // 默认值, 使用PKCS7（兼容PKCS5） padding方式
            $options = 0;
        }

        try {
            $encryptedData = openssl_encrypt(
                $data, $this->method, $this->password, $options, $this->iv
            );
        } catch (Exception $e) {
            $this->lastErrorMessage = $e;

            return false;
        }

        return $encryptedData;
    }

    /**
     * 解密
     *
     * @param string $data 需要解密的字符串
     *
     * @return string|false 失败时返回false
     */
    public function decrypt($data)
    {
        if (empty($data)) {
            return null;
        }
        // 预先 decode
        // base64 url safe decode
        $data = base64_decode(str_pad(
            strtr($data, '-_', '+/'),
            strlen($data) % 4,
            '=',
            STR_PAD_RIGHT
        ));

        if ($this->padding === 'ZERO') {
            $options = OPENSSL_RAW_DATA | OPENSSL_ZERO_PADDING;
        } else {
            // PKCS7（兼容PKCS5）padding，openssl默认方式
            $options = OPENSSL_RAW_DATA;
        }

        try {
            $decryptedData = openssl_decrypt(
                $data, $this->method, $this->password, $options, $this->iv
            );
        } catch (Exception $e) {
            $this->lastErrorMessage = $e;

            return false;
        }

        // 解密后的数据两端可能有padding的空字符, 需要调用方按需trim
        return $decryptedData;
    }

    /**
     * 获取最后一次报错的信息
     *
     * @return string
     */
    public function getLastErrorMessage()
    {
        return sprintf(
            "last error: %s, openssl error: %s",
            $this->lastErrorMessage, openssl_error_string()
        );
    }
}

// 配置回调地址时应回调服务就绪，配置回调地址时会调用配置地址，需要回显才能校验通过
if (!empty($_POST['echostr'])) {
    if (check_signature($signature, $rn, $timestamp, $token)) {
        echo $_POST['echostr'];
    }
    else {
        echo "check signature fail";
    }
} else {
    $msgBase64 = file_get_contents("php://input");
    // 通过AES解密后得到回调消息数据
    $encrypter = new Encrypter(base64_decode($encodingAESKey));
    $decrypted = $encrypter->decrypt($msgBase64);
    $msgData = json_decode(trim($decrypted), true);
}
Click to copy
Node.js示例

const Koa = require('koa');
const bodyParser = require('koa-bodyparser');
const CryptoJS = require('crypto-js');
// 需替换成自己机器人的密匙
const EncodingAESKey = '';
class AESCipher {
    constructor(key) {
        this.key = CryptoJS.enc.Base64.parse(key);
        this.options = {
            mode: CryptoJS.mode.ECB,
            padding: CryptoJS.pad.Pkcs7
        };
    }
    // 加密
    encrypt(data) {
        const cipher = CryptoJS.AES.encrypt(data, this.key, this.options);
        const base64Cipher = cipher.ciphertext.toString(CryptoJS.enc.Base64);
        const resultCipher = base64Cipher
            .replace(/\+/g, '-')
            .replace(/\//g, '_')
            .replace(/=+$/g, '');
        return resultCipher;
    }
    // 解密
    decrypt(content) {
        content = content
            .replace(/-/g, '+')
            .replace(/_/g, '/')
            .padEnd(content.length + content.length % 4, '=')
        const bytes = CryptoJS.AES.decrypt(content, this.key, this.options);
        return bytes.toString(CryptoJS.enc.Utf8);
    }
}
const cipher = new AESCipher(EncodingAESKey);
const app = new Koa();
app.use(bodyParser({enableTypes: ['json', 'form', 'text']}));
app.use(async function (ctx, next) {
    if (ctx.method === 'POST') {
        // 获取POST参数echostr
        if (ctx.request.body.echostr) {
            if (check_signature(signature, rn, timestamp, token)) {
                // 配置回调地址时应回调服务就绪，配置回调地址时会调用配置地址，需要回显才能校验通过
                ctx.body = ctx.request.body.echostr;
            }
            else {
                ctx.body = 'check signaturefail';
            }
        } else {
            // 获取request raw body，不同框架可能略有不同
            let data = ctx.request.rawBody;
            if (!data) {
                console.error('未获取到数据对象');
            }
            // 解密
            data = cipher.decrypt(data);
            console.log('消息体内容', data);
            // 转换为对象
            data = JSON.parse(data);
        }
    } else {
        ctx.body = 'GET request OK'
    }
});
Click to copy
问题定位
企业平台开放接口（https://api.im.baidu.com）会在response header中带上X-Logid，为方便定位，强烈建议业务方在打印调用错误日志时同时打印X-Logid。

全局错误码
返回码	说明
0	请求成功
-1	系统错误
40000	请求参数错误
40001	无权限
40013	不合法的corpid
40020	文件已存在
40021	agentid失效
40022	上报超时
40023	上传成功
40025	文件不完整
40026	fid sessionid 不匹配
40027	个人文件大小超出限制
40028	应用不支持上传
40029	会话id失效/不合法的oauth_code
40030	fid或md5 失效
40031	status ,rate 失效
40032	有风险
40033	当前会话中超过10个文件处理对象
40034	群或讨论组文件过多
40035	群聊ID不合法
40036	群聊非企业群
40037	userid数量超过50
40038	所有userid均失效
40039	设备未找到
40040	参数错误
40041	用户不存在
40042	企业二次认证异常
40043	员工未认证
40044	机器人未被添加到群中
40045	agentId不合法
40046	发送消息频率超限
40047	文件上传失败
40056	不合法的agentid
40060	body超过9k
40061	text类型文本总长度超过2k
40062	link类型单个链接长度超过1k
40063	image类型图片数量超过1个
40064	header中offlinenotify长度超过1k
40065	header中compatible长度超过1k
40066	image类型图片大小超过1m
40067	markdown类型数量超过1个
40068	markdown内容长度超过2048个字符
40069	message属性格式不正确
40071	at超过50人
40080	不合法的suitesecret
40082	不合法的suitetoken
40083	不合法的suiteid
40084	不合法的永久授权码
40085	不合法的suiteticket
40086	重复开启
40087	重复关闭
40200	机器人发送消息权限已被封禁
40201	机器人接受消息权限已被封禁
40300	机器人已被停用
42003	oauth_code超时或者失效
42009	suitetoken失效
60003	部门不存在
60004	父部门不存在
60005	不允许删除有成员的部门
60006	不允许删除有子部门的部门
60007	不允许删除根部门
60008	部门名称已存在
60011	管理员权限不足，(user/department/agent)无权限
60012	非企业获取非群成员
60013	员工未认证


---

## 硬件告警诊断系统 - 如流Webhook使用示例

### 配置说明

在 `.env` 文件中配置如流Webhook参数：

```bash
# 如流Webhook配置
RULIU_WEBHOOK_URL=http://apiin.im.baidu.com/api/v1/robot/msg/groupmsgsend
RULIU_ACCESS_TOKEN=your_access_token_here
RULIU_GROUP_ID=your_group_id_here
```

### Python代码示例

```python
import httpx
import time
from typing import Dict, Any

async def send_ruliu_notification(
    webhook_url: str,
    access_token: str,
    group_id: int,
    content: str
) -> bool:
    """
    发送如流通知
    
    Args:
        webhook_url: 如流Webhook URL
        access_token: 访问令牌
        group_id: 群组ID
        content: Markdown格式的消息内容
        
    Returns:
        是否发送成功
    """
    # 构建URL（access_token作为query参数）
    url = f"{webhook_url}?access_token={access_token}"
    
    # 构建请求头
    headers = {
        'Content-Type': 'application/json'
    }
    
    # 构建消息体
    message = {
        "message": {
            "header": {
                "toid": int(group_id),
                "totype": "GROUP",
                "msgtype": "MD",  # Markdown类型
                "clientmsgid": int(time.time() * 1000),
                "role": "robot"
            },
            "body": [
                {
                    "type": "MD",
                    "content": content
                }
            ]
        }
    }
    
    try:
        async with httpx.AsyncClient(timeout=30.0) as client:
            response = await client.post(url, json=message, headers=headers)
            
            if response.status_code == 200:
                result = response.json()
                if result.get('errcode') == 0 or result.get('code') == 'ok':
                    print(f"✅ 如流通知发送成功")
                    return True
                else:
                    print(f"⚠️ 如流通知响应异常: {result}")
                    return False
            else:
                print(f"❌ 如流通知发送失败: {response.status_code}, {response.text}")
                return False
                
    except Exception as e:
        print(f"❌ 如流通知异常: {str(e)}")
        return False


# 使用示例
async def main():
    # 配置参数
    webhook_url = "http://apiin.im.baidu.com/api/v1/robot/msg/groupmsgsend"
    access_token = "your_access_token"
    group_id = 11930956
    
    # 构建Markdown消息内容
    content = """##### 【硬件告警】GPU带宽下降

**严重程度**: <font color="red">ERROR</font>
**集群ID**: cce-xrg955qz
**组件类型**: gpu
**IP地址**: 10.90.0.235
**告警时间**: 2026-02-10 15:30:00

**诊断结果**:
- 手册匹配: GPU带宽下降
- 危害等级: 严重
- 解决方案: 检查GPU硬件状态,必要时更换GPU

**AI解读**:
GPU报告DriverError_EccError_GSPError_Xid错误，初步判定为GPU带宽下降，可能由硬件故障或驱动异常引发。建议重启节点观察是否恢复，必要时更换GPU。
"""
    
    # 发送通知
    success = await send_ruliu_notification(
        webhook_url=webhook_url,
        access_token=access_token,
        group_id=group_id,
        content=content
    )
    
    if success:
        print("通知发送成功！")
    else:
        print("通知发送失败！")


if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

### 消息格式说明

#### 1. Markdown格式支持

如流Markdown支持以下语法：

- **标题**：`##### 五级标题`
- **粗体**：`**加粗文本**`
- **字体颜色**：`<font color="red">红色</font>`（支持red、green、gray）
- **链接**：`[百度一下](https://www.baidu.com/)`
- **引用**：`> 引用文本`
- **列表**：`- 无序列表` 或 `1. 有序列表`

#### 2. @人功能

```python
# @特定成员
message = {
    "message": {
        "header": {
            "toid": group_id,
            "totype": "GROUP",
            "msgtype": "TEXT",
            "clientmsgid": int(time.time() * 1000),
            "role": "robot"
        },
        "body": [
            {
                "type": "TEXT",
                "content": " 以下人员注意啦～ "
            },
            {
                "type": "AT",
                "atall": False,  # True表示@全体成员
                "atuserids": ["user1", "user2"]  # 成员ID列表
            }
        ]
    }
}
```

### 常见问题

#### Q1: 如何获取access_token？

参考文档：[获取app_access_token](https://qy.baidu.com/doc/index.html#/inner_serverapi/robot)

#### Q2: 如何获取群组ID？

在企业后台 > 应用中心 > 你的机器人 > 功能 > 机器人 > 设置中查看

#### Q3: 消息发送频率限制？

每个机器人每群20条/分钟，超出后将限流5分钟

#### Q4: 返回错误码40046（发送消息频率超限）？

说明发送频率超过限制，需要等待5分钟后重试

### 测试验证

```bash
# 运行测试脚本
python3 test_webhook_notification.py
```

预期结果：
- ✅ 如流群组收到Markdown格式的告警通知
- ✅ 消息包含告警类型、严重程度、诊断结果、AI解读等信息
- ✅ 字体颜色正确显示（红色表示ERROR）
