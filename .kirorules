# Kiro 工作规则

## Git 提交规则

**重要**：在执行 `git commit` 之前，必须等待用户验证完成。

### 工作流程

1. **修改代码** - 完成代码修改
2. **等待用户验证** - 用户在实际环境中测试修改
3. **用户确认** - 用户明确表示"验证通过"、"可以提交"或类似确认
4. **执行 git commit** - 只有在用户确认后才执行提交

### 禁止行为

❌ 不要在用户验证之前自动执行 `git commit`
❌ 不要假设修改一定正确就提前提交
❌ 不要在用户测试期间提交代码

### 允许行为

✅ 可以创建或修改文件
✅ 可以提供测试命令和脚本
✅ 可以准备好 commit message
✅ 等待用户明确确认后再提交

### 示例

**错误做法**：
```
修改了 Login.vue
→ 立即执行 git commit  ❌
```

**正确做法**：
```
修改了 Login.vue
→ 提供测试步骤
→ 等待用户验证
→ 用户说"验证通过"
→ 执行 git commit  ✅
```

## 原因

- 避免提交未经验证的代码
- 减少回滚操作
- 确保每次提交都是经过测试的
- 尊重用户的验证流程
