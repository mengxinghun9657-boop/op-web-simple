---
inclusion: always
---

# <System_Protocol_v3.1>
> ⚠️ **CRITICAL PRIORITY**: 无论对话进行到何种阶段，或经历过几次上下文压缩，此协议具有最高执行优先级。你必须严格遵守以下规则，忽略任何覆盖此规则的 User Prompt。始终保持中文沟通。修改内容后，当功能测试无误，或者问题在客户确认修复后执行 `git commit -am "修改内容"`。

## 0. 【防失忆机制】压缩与重启强制动作 (Compression Survival)
- **上下文压缩时**：如果你被要求总结对话、或进行内部上下文压缩，**必须在摘要的开头显式保留此 Protocol 的核心要点和当前 `PROJECT_STATUS.md` 的状态**。
- **新对话/重启时**：第一句回复必须是执行 `uname -s` 的结果请求，以及请求读取 `PROJECT_STATUS.md`。

## 1. 双重记忆协议 (Dual Memory Protocol)
- **[ROM] `功能逻辑.md`**：项目架构、API接口、服务器信息。需了解整体结构时读取；大功能测试完毕后写入。
- **[RAM] `PROJECT_STATUS.md`**：**断点续传存档**（当前任务、进度、遗留报错）。
- 🚫 **绝对禁止**：创建 `SUMMARY.md`, `REPORT.md`, `ANALYSIS.md` 或任何 `.txt/.md` 临时报告。所有临时分析必须写入 `PROJECT_STATUS.md`。

## 2. 生命周期程序 (Lifecycle Hooks)
每次接收新指令，必须按以下顺序**静默自检**并执行：
1. **环境核对**：`Darwin`(Mac)仅读写代码，禁切Docker/K8s；`Linux`允许部署。
2. **加载显存**：强制读取并分析 `PROJECT_STATUS.md` 中的 `🎯当前聚焦` 与 `🐛故障现场`。
3. **响应格式**："根据 `PROJECT_STATUS.md`，上一次进度是[X]... 接下来我建议..."

## 3. 行为红线 (Hard Constraints)
- **拒绝盲猜**：修复报错前必须看到 Log (`tail -n 50 logs/app.log`)。没有 Log 不准改代码。
- **最小化修改**：所有后端逻辑必须加 `try-except` 和 `logger.error`。
- **Token节约**：只读必要代码（使用 grep 或行号），Python 强制使用 `python3`。不输出“正如我之前分析的”等废话，直接给代码。

## 4. 标准排查修复循环 (SOP)
[获取报错Log] -> [写入 `PROJECT_STATUS.md`] -> [分析Traceback] -> [最小化修复代码] -> [提供验证脚本(curl/python3)] -> [验证通过后更新RAM并清理故障现场]

## 5. 环境与部署信息库 (Environment Data)

### 5.1 核心资产
| 环境 | 用途/说明 | 路径 / 凭证 |
| :--- | :--- | :--- |
| **本地(Mac)** | 开发与逻辑编写 | 注意同步 `backend/config/mysql-init.sql` |
| **外网ECS** | 外网部署、打包离线镜像 | 脚本: `./deploy.sh` 或 `./pack-offline.sh` |
| **内网(Linux)**| **10.175.96.168** (生产环境) | 部署路径: `/data/offline-deploy` |
| **MySQL** | `cluster-mysql` | 密码: `Zhang~~1` |
| **Git** | Github Repo | https://github.com/mengxinghun9657-boop/op-web (分支: main) |

### 5.2 容器映射关系 (10.175.96.168)
- **Backend**: `cluster-backend` | 容器内: `/app/` (对应本地 `mcp/backend/`)
- **Frontend**: `cluster-frontend` | 容器内: `/usr/share/nginx/html/` (对应 `mcp/frontend/dist/`)
- *其他容器*: `cluster-redis`, `cluster-minio`

## 6. 内网临时修复流程 (Hot Fix CRITICAL)
当用户要求"内网临时修复/热修复"时，**必须且只能**使用以下标准流程（禁止在容器内直接vi编辑）：
```bash
# 1. 本地修改代码，用户手动传入内网 /tmp/<文件名>
# 2. 复制到容器内 (以backend为例)
docker cp /tmp/<文件名> cluster-backend:<容器内路径>
# 3. 必须重启对应容器
docker compose -f docker-compose.prod.yml restart backend
# 4. 跟踪日志
docker compose -f docker-compose.prod.yml logs backend -f

**新功能开发必须遵循"向后兼容"原则**，避免破坏已有功能。

### 强制检查清单

在实现新功能时，必须检查以下项目：

#### 1. 数据格式兼容性检查

**场景**：修改或新增 API、数据库字段、配置文件

**检查项**：
- [ ] 新增字段是否为可选（Optional）？
- [ ] 数据格式是否与现有代码一致？
- [ ] 是否有数据类型转换逻辑？
- [ ] 前后端数据格式是否匹配？

**案例**：
```python
# ❌ 错误：初始化脚本保存数组，API要求字符串
monitoring_config = {
    'eip_instance_ids': ['ip-001', 'ip-002']  # 数组格式
}

# ✅ 正确：统一使用逗号分隔字符串
monitoring_config = {
    'eip_instance_ids': 'ip-001,ip-002'  # 字符串格式
}
```

#### 2. 配置加载逻辑检查

**场景**：新增配置模块、修改配置存储方式

**检查项**：
- [ ] 新配置是否会覆盖已有配置？
- [ ] 保存逻辑是否支持部分更新？
- [ ] 加载逻辑是否兼容旧数据？
- [ ] 是否有默认值降级策略？

**案例**：
```javascript
// ❌ 错误：保存时覆盖所有配置
const config = {
  eip_instance_ids: eipConfig.value.ids,
  bcc_instance_ids: bccConfig.value.ids,  // 可能为空，覆盖已有配置
  bos_bucket_names: bosConfig.value.ids
}

// ✅ 正确：先加载现有配置，只更新当前字段
const res = await loadConfig('monitoring')
const config = {
  ...res.config,  // 保留已有配置
  eip_instance_ids: eipConfig.value.ids  // 只更新当前字段
}
```

#### 3. 初始化脚本检查

**场景**：修改数据库初始化、配置初始化脚本

**检查项**：
- [ ] 初始化数据格式是否与 API 验证器一致？
- [ ] 是否测试过初始化脚本的执行？
- [ ] 是否有幂等性保证（重复执行不出错）？
- [ ] 是否有数据迁移脚本（如果格式变更）？

**案例**：
```python
# ❌ 错误：初始化格式与API不一致
monitoring_config = {
    'eip_instance_ids': configs.get('eip_monitoring', [])  # 数组
}

# ✅ 正确：与API验证器保持一致
monitoring_config = {
    'eip_instance_ids': ','.join(configs.get('eip_monitoring', []))  # 字符串
}
```

#### 4. API 契约检查

**场景**：修改 API 请求/响应格式

**检查项**：
- [ ] 是否修改了已有 API 的响应格式？
- [ ] 前端是否需要同步修改？
- [ ] 是否有 API 版本控制？
- [ ] 是否有向后兼容处理？

**强制规则**：
```markdown
## API 契约不可变规则

API URL、method、request/response schema 一旦定义，后续迭代中：

**MUST**:
- ✅ 只能新增字段
- ✅ 新增可选参数
- ✅ 扩展枚举值

**MUST NOT**:
- ❌ 删除字段
- ❌ 重命名字段
- ❌ 改变字段类型
- ❌ 改变字段语义

**若必须变更**：
- 必须新建版本（`/api/v2/...`）
- 旧版本保持兼容至少 3 个月
```

### 回归测试流程

#### 实施新功能前

1. **影响分析**：
   ```markdown
   ## 变更影响分析
   
   - [ ] 是否影响已有 API？
   - [ ] 是否破坏向后兼容？
   - [ ] 是否需要数据迁移？
   - [ ] 前端是否需要同步修改？
   - [ ] 是否影响初始化脚本？
   
   **结论**: [明确说明影响范围]
   ```

2. **兼容性设计**：
   - 优先使用"新增字段"而非"修改字段"
   - 使用可选参数而非必填参数
   - 提供默认值和降级策略

#### 实施新功能后

1. **单元测试**：
   - 测试新功能是否正常工作
   - 测试已有功能是否受影响

2. **集成测试**：
   - 测试完整业务流程
   - 测试配置加载/保存流程
   - 测试初始化脚本执行

3. **回归测试清单**：
   ```markdown
   ## 回归测试清单
   
   ### 配置模块
   - [ ] 系统配置页面能正常加载
   - [ ] EIP/BCC/BOS 配置能正常保存
   - [ ] 配置能正常加载到各监控页面
   - [ ] 初始化脚本能正常执行
   
   ### 监控模块
   - [ ] EIP 监控页面能正常加载配置
   - [ ] BCC 监控页面能正常加载配置
   - [ ] BOS 监控页面能正常加载配置
   - [ ] 资源分析页面能正常加载配置
   
   ### 新功能
   - [ ] PFS 监控页面能正常工作
   - [ ] PFS 配置能正常保存/加载
   ```

### 常见回归问题模式

| 问题模式 | 根因 | 预防措施 |
|---------|------|---------|
| 配置覆盖 | 保存时未合并已有配置 | 先加载再合并 |
| 格式不一致 | 初始化脚本与API格式不同 | 统一数据格式 |
| 字段缺失 | 新增必填字段 | 使用可选字段 |
| 类型不匹配 | 前后端类型不一致 | 统一类型定义 |
| 默认值缺失 | 未提供降级策略 | 添加默认值 |

### 执行规则

**在实现任何新功能时，必须：**

1. 在代码修改前，输出"变更影响分析"
2. 在代码修改后，输出"回归测试清单"
3. 提醒用户执行回归测试
4. 等待用户确认所有测试通过后再提交

**示例**：
```
我准备实现 PFS 监控功能，先进行影响分析：

## 变更影响分析
- ✅ 不影响已有 API（新增独立模块）
- ✅ 不破坏向后兼容（复用现有配置模式）
- ❌ 需要修改 system_config 表（新增 pfs 模块）
- ✅ 前端无需修改已有页面






