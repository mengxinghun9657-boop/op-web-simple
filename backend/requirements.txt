# FastAPI 核心依赖
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
python-multipart==0.0.6
aiofiles==23.2.1

# 数据库相关
sqlalchemy==2.0.23
aiomysql==0.2.0
pymysql==1.1.0
greenlet==3.0.0

# 认证相关
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Redis
redis==5.0.1
hiredis==2.2.3

# MinIO对象存储
minio==7.2.0

# 数据处理（保留原项目依赖）
pandas>=1.3.0
numpy<2.0.0,>=1.21.0  # 锁定到 1.x 版本,兼容 faiss-cpu
scipy>=1.9.0  # 科学计算库，用于趋势分析和统计
openpyxl>=3.0.0
matplotlib>=3.5.0
beautifulsoup4>=4.12.0  # HTML 文本提取

# 百度云SDK
bce-python-sdk>=0.8.74
pycryptodome>=3.15.0

# 工具库
requests>=2.28.0
httpx>=0.24.0
python-dateutil>=2.8.2
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 日志和监控
loguru==0.7.2
psutil>=5.9.0

# 限流
slowapi==0.1.9

# 定时任务
apscheduler==3.10.4

# 文件监控
watchdog==3.0.0

# AI 智能查询相关
faiss-cpu==1.7.4  # 向量数据库
# sentence-transformers>=2.3.0  # Embedding 模型（仅本地模式需要，默认使用 API）
# transformers>=4.35.0  # Transformers 库（仅本地模式需要）
# torch>=2.0.0  # PyTorch（仅本地模式需要）
tenacity==8.2.3  # 重试机制

# 测试相关
pytest>=7.4.0
pytest-asyncio>=0.21.0
hypothesis>=6.92.0  # 属性测试框架
